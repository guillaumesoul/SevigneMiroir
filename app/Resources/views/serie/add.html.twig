{% extends 'base.html.twig' %}

{% block body %}
    <div class="mdl-layout__content">
        <div>
            <h3>Ajout d'une série</h3>
            {{ form_start(form) }}
            {{ form_widget(form) }}
            {{ form_end(form) }}
        </div>
    </div>

{% endblock %}
