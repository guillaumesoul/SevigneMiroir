{% extends 'base.html.twig' %}

{% block body %}
    <div>
        {# TODO affichage des affichages sous forme de vignettes #}
        <h1>Liste des affichages</h1>
        <ul>
            {% for affichage in affichages %}
                <a href="/presentation/manage/{{ affichage.id }}">{{ affichage.name }}</a>
            {% endfor %}
        </ul>
    </div>
    <div>

        <h1>Liste des présentations</h1>
        <ul id="sortable">
            {% for presentation in presentations %}
                <li class="ui-state-default" id="{{ presentation.id }}"><span class="ui-icon ui-icon-arrowthick-2-n-s"></span>{{ presentation.name }}</li>
            {% endfor %}
        </ul>
    </div>
    <div>
        <button id="getOrder">Get Order</button>
    </div>
{% endblock %}


